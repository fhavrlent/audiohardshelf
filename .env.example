# Audiobookshelf configuration
ABS_URL=http://your-audiobookshelf-server:port
ABS_API_KEY=your_audiobookshelf_api_key
ABS_USER_ID=your_audiobookshelf_user_id

# Hardcover.app configuration
HARDCOVER_API_KEY=your_hardcover_api_key
# Note: You can include "Bearer " prefix in your API key or leave it out - the app will handle either format

# Sync configuration
SYNC_INTERVAL="0 */1 * * *"  # Every hour by default (can also use interval in minutes, e.g., "60")

# API Configuration (Optional)
# Timeout for API requests in milliseconds (default: 10000 = 10 seconds)
# API_TIMEOUT_MS=10000

# Logging Configuration (Optional)
# Log level: error, warn, info, debug (default: debug)
# LOG_LEVEL=debug

# Log retention: Keep logs for specified duration (default: 14d)
# Examples: 7d (7 days), 30d (30 days), 60d (60 days)
# LOG_MAX_FILES=14d

# Maximum log file size before rotation (default: 20m)
# Examples: 10m (10 megabytes), 50m (50 megabytes), 100m (100 megabytes)
# LOG_MAX_SIZE=20m
